name = "zeeapply-api"
main = "src/index.js"
compatibility_date = "2024-12-01"

# Account ID is picked up automatically after `wrangler login`.
# Worker is served at:
#   https://zeeapply-api.<your-subdomain>.workers.dev
# Gemini API key is stored as a Wrangler secret:
#   wrangler secret put GEMINI_API_KEY
